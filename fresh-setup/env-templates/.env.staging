# =============================================
# Ojo STAGING ENVIRONMENT
# For QA testing before production
# =============================================

# Supabase Configuration (Staging)
# Separate project from production for safe testing
NEXT_PUBLIC_SUPABASE_URL=https://your-staging-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-staging-anon-key-here

# World ID Configuration (Staging App)
# Can use same app as development or separate
NEXT_PUBLIC_APP_ID=app_staging_your-app-id

# Payment Configuration (Staging Wallet)
# Use a separate wallet from production
NEXT_PUBLIC_OWNER_WALLET=0x_your_staging_wallet_address

# Admin Configuration
NEXT_PUBLIC_ADMIN_ID=your_staging_admin_nullifier_hash

# =============================================
# STAGING NOTES
# =============================================
# - Uses Supabase project: __APP_NAME_LOWER__-staging
# - Mirror of production environment
# - Used for QA testing and demos
# - Real World ID verification required
# - Payments go to staging wallet
# - Deploy from 'staging' git branch
# =============================================
